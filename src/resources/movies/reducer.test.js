import MoviesReducer from './reducer';
const initialState = {
  byId: {}
};

describe('MoviesReducer', () => {
  // it.only('should return the initialState', () => {
  //   expect(MoviesReducer(undefined, {})).toEqual(initialState);
  // });
});
